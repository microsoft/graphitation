subscription TodoUpdatesSubscription($limit: Int!) {
  emitTodos(limit: $limit) {
    id
    ...TodoSubscriptionFragment
  }
}

fragment TodoSubscriptionFragment on Todo {
  id
  text
  isCompleted
}
