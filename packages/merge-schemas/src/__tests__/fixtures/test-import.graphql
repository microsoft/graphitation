extend schema
  @import(
    from: "@graphitation/merge-schema-testing-core-graphql"
    defs: ["Node", "DateTime", "Person"]
  )

type Foo implements Node {
  id: ID!
  timestamp: DateTime
}

type Bar implements Node {
  id: ID!
}

extend type Person {
  foo: Foo
}
