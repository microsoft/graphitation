query UserProfile($userId: ID!) {
  user(id: $userId) {
    id
    name
    email
    profile {
      bio
      avatar
      createdAt
    }
    posts(first: 10) {
      id
      title
      content
      createdAt
      likes
    }
  }
}