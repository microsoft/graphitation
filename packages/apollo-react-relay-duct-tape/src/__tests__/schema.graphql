type Query {
  user(id: ID!): User!
}

type Mutation {
  updateUserName(id: ID!, name: String!): User!
}

type Subscription {
  userNameChanged(id: ID!): User!
}

type User {
  id: ID!
  name: String!
}
