// This export in DT is wrong, but it's an old version and relay-compiler
// no longer exists as JS, so whatever.
import { CompilerContext } from "relay-compiler/lib/core/CompilerContext";
export default CompilerContext;
