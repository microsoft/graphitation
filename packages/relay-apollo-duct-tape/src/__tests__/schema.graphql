type Query {
  conversation(id: String!): Conversation!
  node(id: ID!): Node
}

interface Node {
  id: ID!
}

type Conversation implements Node {
  id: ID!
  title: String!
  hasUnreadMessages: Boolean!
}
