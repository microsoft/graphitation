query CacheTestConversationKeyFieldsWithGarbageInput(
  $id: String!
  $callerInfo: String
  $moreCallerInfo: String
) {
  conversationWithNested(
    input: { id: $id, callerInfo: $callerInfo }
    moreCallerInfo: $moreCallerInfo
  ) {
    id
    title
  }
}
